# The extension ships only the bundled dist + manifest + docs.
# Everything else is source/build tooling and stays out of the .vsix.
src/**
test/**
node_modules/**
.vscode/**
esbuild.mjs
tsconfig.json
package-lock.json
.gitignore
**/*.map
HANDOFF.md
REPORT.md
